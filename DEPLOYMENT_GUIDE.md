# 🚀 Пошаговая Инструкция по Deployment GuideFarm Bot

## ⚠️ ВАЖНО: Перед деплоем

### Шаг 1: Убедитесь, что workspace bot ОСТАНОВЛЕН

**В правой панели Replit:**
1. Найдите панель "Telegram Bot" workflow
2. Нажмите кнопку **"Stop"** (красная кнопка остановки)
3. Дождитесь полной остановки (статус должен измениться на "Stopped")

**Если кнопка Stop не помогает:**
- Откройте Shell в Replit
- Выполните команду: `pkill -9 -f "python main.py"`
- Подождите 5 секунд

### Шаг 2: Проверьте, что бот действительно остановлен

**В Shell выполните:**
```bash
ps aux | grep "python main.py" | grep -v grep
```

**Должно быть пусто!** Если видите процесс - повторите Шаг 1.

### Шаг 2.5: Проверьте наличие всех Secrets

**Обязательные secrets:**
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота

**AI Provider secrets (хотя бы один):**
- `OPENAI_API_KEY` - для GPT-4o
- `GEMINI_API_KEY` - для Gemini 1.5 Flash  
- `XAI_API_KEY` - для Grok 3

**Ozon API credentials (для публикации):**
- `OZON_CLIENT_ID`
- `OZON_API_KEY`

**Как проверить:**
1. Откройте раздел **"Secrets"** в левой панели Replit
2. Убедитесь что все необходимые secrets добавлены
3. Secrets автоматически передаются в deployment

---

## 📦 Deployment на Reserved VM

### Шаг 3: Начните Deployment

1. Нажмите кнопку **"Publish"** в верхнем правом углу Replit
2. В открывшемся окне выберите **"Reserved VM Deployment"** 

⚠️ **НЕ ВЫБИРАЙТЕ "Autoscale Deployment"** - это не работает для Telegram ботов!

### Шаг 4: Настройте Deployment

**В настройках deployment укажите:**

1. **App type:** `Background worker`
   - Бот не предоставляет HTTP сервер
   - Он работает в фоновом режиме 24/7
   
2. **Run command:** (уже настроено автоматически)
   - `python main.py`

3. **Environment variables:** (автоматически)
   - Все secrets (TELEGRAM_BOT_TOKEN, OPENAI_API_KEY и др.) автоматически передаются в deployment

### Шаг 5: Подтвердите и запустите

1. Проверьте все настройки
2. Нажмите **"Deploy"** или **"Publish"**
3. Дождитесь завершения deployment (обычно 2-5 минут)

---

## ✅ Проверка успешного Deployment

### После успешного deployment:

1. **Проверьте логи deployment:**
   - В панели deployment должны быть логи запуска бота
   - Ищите строки:
     ```
     🚀 Запуск GuideFarm Bot...
     ✅ Бот инициализирован
     🤖 Запуск в режиме polling...
     Application started
     ```

2. **Проверьте работу бота:**
   - Откройте Telegram
   - Найдите вашего бота
   - Отправьте команду `/start`
   - Бот должен ответить приветственным сообщением

---

## 🔧 Решение проблем

### Проблема: "Multiple bot instances conflict"

**Решение:**
1. Остановите deployment (нажмите "Stop" в панели deployment)
2. Вернитесь к Шагу 1 этой инструкции
3. Убедитесь, что workspace bot ПОЛНОСТЬЮ остановлен
4. Подождите 30 секунд
5. Повторите deployment

### Проблема: "Autoscale Deployment is incompatible"

**Решение:**
- Вы выбрали **неправильный тип deployment**
- Удалите текущий deployment
- Создайте новый, выбрав **Reserved VM** (НЕ Autoscale)
- App type: **Background worker**

### Проблема: "No open port detected"

**Решение:**
- Это нормально! Telegram bot не открывает порты
- Убедитесь, что выбран **Background worker** (НЕ "Web service")
- Reserved VM не требует открытых портов для background workers

### Проблема: "TELEGRAM_BOT_TOKEN not found" или "ConfigurationError"

**Решение:**
1. Откройте **Secrets** в левой панели Replit
2. Убедитесь что `TELEGRAM_BOT_TOKEN` добавлен
3. **Для deployment**: Secrets автоматически передаются из Replit Secrets в production
4. Если secret присутствует но не работает:
   - Остановите deployment
   - Проверьте что secret правильно назван (без опечаток)
   - Пересоздайте deployment
   
**Что делает бот при запуске:**
- Проверяет наличие всех обязательных secrets
- Выводит статус конфигурации (какие secrets загружены)
- Если чего-то не хватает - показывает понятное сообщение об ошибке

---

## 📊 Технические детали

### Почему Reserved VM?

- ✅ Telegram bot использует **polling mode** (не webhooks)
- ✅ Polling требует **постоянного соединения** 24/7
- ✅ Reserved VM обеспечивает **99.9% uptime**
- ✅ **Одна инстанция** - никаких конфликтов

### Почему НЕ Autoscale?

- ❌ Autoscale требует HTTP health checks
- ❌ Autoscale может scale down to zero
- ❌ Polling bot отключится при scaling
- ❌ Множественные инстансы конфликтуют

### File Lock Mechanism

- В **development** (workspace): Lock активен - предотвращает запуск двух ботов
- В **production** (Reserved VM): Lock автоматически отключен - VM гарантирует одну инстанцию

---

## 🎯 Чеклист перед Deployment

- [ ] Workspace bot полностью остановлен (проверено в Shell)
- [ ] Выбран **Reserved VM Deployment** (НЕ Autoscale)
- [ ] App type: **Background worker** (НЕ Web service)
- [ ] Все secrets настроены (TELEGRAM_BOT_TOKEN, API keys)
- [ ] Подождал 30 секунд после остановки workspace бота

---

## 📞 Поддержка

Если deployment всё ещё не работает:
1. Проверьте логи deployment на наличие ошибок
2. Убедитесь, что TELEGRAM_BOT_TOKEN корректный
3. Проверьте, что у вас достаточно credits для Reserved VM
4. Свяжитесь с поддержкой Replit если проблема persists
